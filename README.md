Movie repository website using react and API calls

To run app; add a .env file in the project root and add this line: REACT_APP_API_URL="api_key"

You can get the api key from https://www.omdbapi.com/